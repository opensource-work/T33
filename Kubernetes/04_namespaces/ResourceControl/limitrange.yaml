apiVersion: v1 
kind: LimitRange 
metadata:
  name: dev-team-limit-range 
  namespace: dev-team 
spec:
  limits:
  - type: Pod 
    min: 
      cpu: "100m"
      memory: "100Mi"
    max:
      cpu: "200m"
      memory: "200Mi"