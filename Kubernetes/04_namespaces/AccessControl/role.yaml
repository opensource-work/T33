apiVersion: rbac.authorization.k8s.io/v1
kind: Role              # Namespace level access control
metadata:
  name: pods-and-podlog-reader
  namespace: dev-team 
rules:
- apiGroups:
    - ""
  resources:
    - pods 
    - pods/log
  verbs: ["get","list"]